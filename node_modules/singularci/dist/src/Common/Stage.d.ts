import Job, { JobSyntaxType } from './Job';
export declare type StageSyntaxType = {
    name: string;
    runs_on: string;
    needs?: string[];
    jobs: JobSyntaxType[];
};
export default class Stage {
    private name;
    private jobs;
    private needs;
    private runs_on;
    constructor(name: string, jobs: Job[], needs: string[], runs_on: string);
    getName(): string;
    getJobs(): Job[];
    getNeeds(): string[];
    getRunsOn(): string;
}
//# sourceMappingURL=Stage.d.ts.map